There are two ways to add a new media file (an image, audio file, video file, or any other file):

## Lisää uusi itsenäinen mediafile

Lisätäksesi itsenäisen mediafile, napsauta + kuvaketta sovelluksen yläosassa ja valitse "Media Object".

Napsauta "valitse tiedosto" valitaksesi tiedoston tietokoneeltasi. Mobiililaitteella tämän painikkeen napsauttaminen antaa sinulle mahdollisuuden ottaa suoraan kuva laitteen kameralla.

Valinnaisesti,

- syötä mediafilelle kuvaus "otsikon" alle
- syötä päivämäärä
- aseta mediafile yksityiseksi (mikä tekee siitä näkyvän vain riittävän valtuutuksen omaaville käyttäjille)

Napsauta "lisää" ladataksesi tiedoston ja luodaksesi mediaobjektin.

## Lisää uusi mediafile ja linkitä se toiseen objektiin

Seuraavat objektityypit Grampsissa voivat sisältää liitettyjä mediaobjekteja: ihmiset, perheet, tapahtumat, paikat, lähteet ja viittaukset.

Minkä tahansa objektin yksityiskohtaisessa näkymässä napsauta sinistä muokkauspainiketta oikeassa alakulmassa (jos et näe sitä, käyttäjälläsi ei ole muokkausoikeuksia). Napsauta "galleria" välilehteä ja napsauta sinistä + painiketta.

Avautuu dialogi, joka tarjoaa samat kentät kuin edellisessä osiossa kuvattiin. Napsauta "tallenna" ladataksesi tiedoston, lisätäksesi uuden mediaobjektin ja linkittääksesi sen tarkasteltavaan objektiin.

## Tekstin tunnistus (OCR)

Jos palvelimen ylläpitäjä on ottanut käyttöön OCR-tuen, "Tekstin tunnistus" -painike ilmestyy kuvan alle mediaobjektin yksityiskohtaisessa näkymässä.

Napsauta "Tekstin tunnistus", valitse kuvassa näkyvän tekstin kieli ja napsauta sitten "Suorita". Kuva käsitellään [Tesseract OCR](https://github.com/tesseract-ocr/tesseract) -ohjelmalla ja tunnistettu teksti näytetään alla.

Jos käyttäjälläsi on muokkausoikeudet, napsauta "Tallenna muistiinpanona" luodaksesi uuden muistiinpanon (tyypiltään "Transkriptio"), joka sisältää tunnistetun tekstin ja linkittää sen mediaobjektiin.

!!! tip
    OCR:n tarkkuus riippuu voimakkaasti kuvan laadusta ja valitusta kielestä. Jos tulos näyttää väärältä, kokeile toista kieltä – esimerkiksi historialliset saksalaiset asiakirjat tarvitsevat usein Fraktur-muunnoksen tavallisen saksan sijaan.
