---
hide:
  - toc
---

# API 规范

交互式 API 规范 (Swagger UI) 直接由您的 Gramps Web 实例提供，地址为 `/api/swagger-ui` – 例如 `https://mygrampsweb.example.com/api/swagger-ui`。

您也可以通过 **启动 Swagger** 按钮从 [用户设置](../../user-guide/settings.md) 访问它，该按钮会打开 Swagger UI，并预填充您的会话令牌。
