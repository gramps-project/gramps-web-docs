# 最近浏览的对象

侧边栏中的 **历史** 打开最近浏览的对象页面，该页面列出了您在 Gramps Web 中打开的最后对象，最近查看的对象位于顶部。最多记住 20 个对象。

这对于快速导航回您在会话中之前查看的某个人、家庭或其他对象非常有用，而无需再次搜索。

点击任何条目以返回该对象。使用 **清除所有** 按钮重置列表。

历史记录存储在浏览器的本地存储中 - 它特定于当前设备和浏览器，并且不与其他用户共享。
